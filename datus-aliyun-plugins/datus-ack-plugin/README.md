# datus-ack-plugin

Read-only Alibaba Cloud ACK inspection and temporary Kubernetes authentication
for `datus-k8s-plugin`. The plugin does not invoke the Alibaba Cloud CLI and
does not persist kubeconfig or bearer tokens.
