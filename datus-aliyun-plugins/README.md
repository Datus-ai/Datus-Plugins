# Datus Alibaba Cloud plugins

`datus-ack-plugin` inspects ACK and supplies temporary credentials to
`datus-k8s-plugin`. Alibaba OSS uses the existing `datus-s3-plugin` with
`compatibility: aliyun-oss`; no duplicate OSS distribution is shipped.
