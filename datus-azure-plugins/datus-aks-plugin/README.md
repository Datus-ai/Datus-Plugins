# datus-aks-plugin

Read-only AKS inspection and Entra ID token authentication for
`datus-k8s-plugin`, without invoking `az` or `kubelogin`. Azure public, China,
and government authority/resource-manager endpoints are supported.
