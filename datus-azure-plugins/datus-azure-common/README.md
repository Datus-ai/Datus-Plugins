# datus-azure-common

Internal library shared by the Datus AKS and ADLS plugins. It provides Azure
Identity construction, sovereign-cloud settings, output rendering and CLI
error handling. It has no `datus.plugins` entry point.
