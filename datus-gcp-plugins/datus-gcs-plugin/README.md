# datus-gcs-plugin

Browse, preview and move GCS data, create signed URLs and inspect or replace
bucket lifecycle rules. Authentication uses ADC, credential files or service
account impersonation through `datus-gcp-common`.
