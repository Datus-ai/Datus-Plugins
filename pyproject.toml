# Virtual workspace root — not an installable package.
# datus-airflow-plugin and datus-statsig-plugin are published independently;
# datus-aws-plugins bundles all AWS plugins + the shared datus_aws_common layer
# into a single distribution.
[tool.uv.workspace]
members = ["datus-*-plugin", "datus-aws-plugins"]
